import React from 'react';
import { NavLink } from 'react-router-dom';
import Nav from './Nav';

const Header = () => {
    return (
        <header>
            <h1>Interact with Car Shopping API!</h1>
        </header>
    )
}

export default Header;
